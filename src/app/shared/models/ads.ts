export interface Ad {
  imageUrl: string; // Background image
  title: string; // Title of the announcement
  description?: string; // Description of the announcemente (Optional)
  link?: string; // URL where the ad redirects (optional)
}
